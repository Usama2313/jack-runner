{
  "name": "jack-runner-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/src/index.js",
    "client": "cd client && npm run dev",
    "server": "node server/src/index.js"
  }
}
